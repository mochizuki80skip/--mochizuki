{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "git": {
    "deploymentEnabled": {
      "main": true,
      "claude/nutrition-fitness-tracker-Mvt0M": true
    }
  },
  "ignoreCommand": "bash -c 'git log -1 --name-only --pretty=format: HEAD | grep -q \"^nutrition-app/\" && exit 1 || exit 0'"
}
